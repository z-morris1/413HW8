To compile the program (won't need to add dependency classes since they're in the same directory):
javac DeltaDebug.java

To generate the diff file:
diff -U 0 file1v1.java file1v2.java > diff

To run:
java DeltaDebug file1v1.java file1v2.java diff